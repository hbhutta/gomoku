// import { writeFile } from "fs";
export default function recordMove(currentPlayer: boolean, column_id: number, row_id: number) {
    /*
    1. Read the JSON file into an array
    2. Push the new move to this array
    3. Convert the array back into JSON
    4. Write the array back to the file
    */
    // const data = `${currentPlayer ? "Black" : "White"} played at (${row_id}, ${column_id})`
    // writeFile('../data/move_history.json', data, (err) => {
    //     if (err) throw err;
    //     console.log('The file has been saved!');
    // });
}